# iterate through each subdirectory and execute the arguments supplied
# e.g "dodir.sh pwd" will execute the pwd command in each subdirectory
# e.g "dodir.sh touch somefile" will create a new file in each subdir